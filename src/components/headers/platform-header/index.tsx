import { cn } from "@/lib/utils";

export const PlatformHeader = () => {
  return (
    <header
      data-testid="platform-header"
      className={cn(
        "bg-red-100 fixed top-0 left-0 right-0 z-10 flex items-center justify-between max-w-[1408px] w-full mx-auto py-5 md:py-2 px-5 xl:px-0"
        // hideElements && "max-w-full py-5 xl:py-8! px-5 xl:px-10! h-auto"
      )}
    >
      xxx
    </header>
  );
};
